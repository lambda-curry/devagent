# Build Workflow (Command)

## Instructions

1. Required inputs (provide in Input Context): target agent purpose, scope boundaries, expected artifacts with primary storage path/pattern.

2. Optional inputs: known resources (peer agents, tools, APIs), prior art/reference prompts, project-specific constraints, adoption environment considerations.

3. Using only `.devagent/**`, follow the workflow steps and write outputs under `.devagent/workspace/` as the workflow specifies.

4. Follow the `.devagent/core/workflows/build-workflow.md` workflow and execute it based on the following input:

---

**Input Context:**
