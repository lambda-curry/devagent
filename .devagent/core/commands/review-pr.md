# Review PR (Command)

## Instructions

1. Required inputs (provide in Input Context): PR number or PR URL.

2. Optional inputs: task hub path, Linear issue IDs, review focus areas, GitHub repository (if not inferred from context).

3. Using only `.devagent/**`, follow the workflow steps and write outputs under `.devagent/workspace/` as the workflow specifies.

4. Follow the `.devagent/core/workflows/review-pr.md` workflow and execute it based on the following input:

---

**Input Context:**
