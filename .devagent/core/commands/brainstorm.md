# Brainstorm (Command)

## Instructions

1. Required inputs (provide in Input Context): brainstorm topic/problem statement, desired mode (`exploratory`, `targeted`, or `expansion`), mission context (from `workspace/product/mission.md`), known constraints (technical, compliance, budget, timeline).

2. Optional inputs: prior brainstorm results, user feedback or pain points, competitive intelligence, stakeholder perspectives to explore, guiding questions from `workspace/product/guiding-questions.md`.

3. This workflow runs as an interactive session; confirm missing required inputs and mode choice before proceeding.

4. Using only `.devagent/**`, follow the workflow steps and write outputs under `.devagent/workspace/` as the workflow specifies.

5. Follow the `.devagent/core/workflows/brainstorm.md` workflow and execute it based on the following input:

---

**Input Context:**
