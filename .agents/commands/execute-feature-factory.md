# Execute Feature Factory (Command)

## Instructions

1. Using only `.devagent/**`, follow the `.devagent/core/workflows/execute-feature-factory.md` workflow to brainstorm and execute multiple improvements automatically.

2. Execute the `brainstorm` workflow first in `exploratory` mode.

3. Select the top 5 candidates and run `execute-full-task` for each sequentially.

4. Push the limits of automation while maintaining full traceability in task hubs.

---

**Input Context:**
Goal: "Improve the DevAgent system and push the limits of automated feature development."
Number of features: 5
Task Complexity: complex
