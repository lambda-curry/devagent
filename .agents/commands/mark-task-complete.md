# Mark Task Complete (Command)

## Instructions

1. Required inputs (provide in Input Context): task directory path (e.g., `.devagent/workspace/tasks/active/YYYY-MM-DD_task-slug/`) or task slug to locate under `active/`.

2. Optional inputs: completion notes to add to the task progress log.

3. Using only `.devagent/**`, follow the workflow steps and write outputs under `.devagent/workspace/` as the workflow specifies.

4. Follow the `.devagent/core/workflows/mark-task-complete.md` workflow and execute it based on the following input:

---

**Input Context:**
