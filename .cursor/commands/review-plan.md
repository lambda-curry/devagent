# Review Plan (Command)

## Instructions

1. Required inputs (provide in Input Context): plan document path (or plan document content).

2. Optional inputs: review scope preference (specific sections, full walkthrough, or adaptive), known concerns or areas to focus on.

3. This workflow runs as an interactive review session; analyze the plan structure, present a high-level summary, and guide users through reviewing specific sections or walking through the entire plan step-by-step.

4. Using only `.devagent/**`, follow the workflow steps and write outputs under `.devagent/workspace/` as the workflow specifies.

5. Follow the `.devagent/core/workflows/review-plan.md` workflow and execute it based on the following input:

---

**Input Context:**
