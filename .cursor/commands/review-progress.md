# Review Progress (Command)

## Instructions

1. Required inputs (provide in Input Context): work artifact (task prompt, spec, plan, or freeform description as a file path or inline content) and current progress state (completed, in progress, blocked).

2. Optional inputs: code file references, related PRs or commits, known issues or questions, target resume date.

3. Using only `.devagent/**`, follow the workflow steps and write outputs under `.devagent/workspace/` as the workflow specifies.

4. Follow the `.devagent/core/workflows/review-progress.md` workflow and execute it based on the following input:

---

**Input Context:**
