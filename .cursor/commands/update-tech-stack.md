# Update Tech Stack (Command)

## Instructions

1. Required inputs (provide in Input Context): repository or project context (name, purpose, target domain) and location for the tech stack file (default: `.devagent/workspace/memory/tech-stack.md`).

2. Optional inputs: existing package manifests (package.json, requirements.txt, pyproject.toml, Gemfile, go.mod) and project documentation (README, architecture docs).

3. Using only `.devagent/**`, follow the workflow steps and write outputs under `.devagent/workspace/` as the workflow specifies.

4. Follow the `.devagent/core/workflows/update-tech-stack.md` workflow and execute it based on the following input:

---

**Input Context:**
